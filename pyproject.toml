[project]
name = "openai-agents-sdk-tool-usage-demo"
version = "0.1.0"
description = "Beginner demo of tool usage in the OpenAI Agents SDK."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai-agents>=0.6.0",
    "python-dotenv>=1.0.1",
]

[tool.uv]
package = false
